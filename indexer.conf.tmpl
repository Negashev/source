input {
  redis {
    host => "{{ default .Env.REDIS_HOST "redis" }}"
    port => 6379
    threads => 8
    key => "logstash"
    data_type => "list"
    codec => json
    batch_count => 20000
  }
}
output {
  elasticsearch {
    hosts => "elasticsearch"
    index => "logstash-%{filename}"
    workers => 8
  }
}
