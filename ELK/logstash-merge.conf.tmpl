input {
  elasticsearch {
    hosts => ["elasticsearch"]
    index => "{{ .Env.SOURCE }}"
    size => 500
    scroll => "5m"
    docinfo => true
  }
}
output {
  elasticsearch {
    hosts => ["elasticsearch"]
    index => "{{ .Env.MERGE }}"
    document_type => "%{[@metadata][_type]}"
    document_id => "%{[@metadata][_id]}"
  }
}
