input {
  elasticsearch {
    hosts => ["elasticsearch"]
    index => "{{ .Env.SOURCE }}"
  }
}
output {
  elasticsearch {
    index => "{{ .Env.MERGE }}"
    document_type => "%{[@metadata][_type]}"
    document_id => "%{[@metadata][_id]}"
  }
}
