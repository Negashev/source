input {
  elasticsearch {
    hosts => "elasticsearch"
    index => "{{ .Env.SOURCE }}"
    query => "*"
    size => 500
    scroll => "5m"
    docinfo => true
  }
}
output {
  elasticsearch {
    hosts => "elasticsearch"
    index => "{{ .Env.MERGE }}"
    workers => "16"
  }
}
