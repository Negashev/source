input {
  file {
    path => "/load/*/*"
  }
}
filter {
  grok {
    patterns_dir => ["/config/pattern/pattern"]
    match => { "message" => "%{FJY_BY_NEGASH}" }
  }
}
output {
  stdout { codec => rubydebug }
}
