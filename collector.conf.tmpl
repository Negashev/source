input {
  file {
    path => "/load/*/*"
  }
}
filter {
  grok {
    patterns_dir => ["/config/pattern"]
    match => { "message" => "%{FJY_BY_NEGASH}" }
  }
}
output {
  stdout { codec => rubydebug }
}
