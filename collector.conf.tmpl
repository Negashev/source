input {
  file {
    path => "/load/*/*"
  }
}
filter {
  csv {csv {
      columns => ["ts","uid","orig_h","orig_p","resp_h","resp_p","proto","service"]
      separator => "	"
    }
  }
}
output {
  stdout { codec => rubydebug }
}
