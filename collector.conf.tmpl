input {
  file {
    path => "/load/*/*"
  }
}
filter {
  grok {
    patterns_dir => ["/config/pattern"]
    csv {
        separator => "${TAB_EXPLODE}"
    }
  }
}
output {
  stdout { codec => rubydebug }
}
